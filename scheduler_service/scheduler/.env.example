# scheduler required env vars

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional — defaults shown
JOB_MAX_RETRIES=5
JOB_BASE_BACKOFF_SECONDS=30
JOB_MAX_BACKOFF_SECONDS=3600
JOB_CLAIM_TIMEOUT_MINUTES=15
JOBS_TABLE=scheduled_jobs
RECURRING_JOBS_TABLE=recurring_jobs
